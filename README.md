# Poker REST API

Полнофункциональный REST API для игры в покер с категориями столов LOW, MID, VIP, использующий PostgreSQL, Kafka, Redis и авторизацию через Telegram Web App.

## Возможности

- 🎮 **Полная игра в покер** - Texas Hold'em с полной логикой игры
- 🔐 **Авторизация Telegram** - Безопасная авторизация через Telegram Web App
- 🗄️ **PostgreSQL** - Надежное хранение данных
- 📨 **Kafka** - Реальное время событий игры
- ⚡ **Redis** - Быстрое кэширование состояний игры
- 🎯 **Автоматическое управление столами** - Создание столов по мере необходимости
- 🎲 **Категории столов** - LOW, MID, VIP с разными лимитами
- 💰 **Управление балансом** - Автоматическое управление фишками и балансом
- 📊 **Мониторинг в реальном времени** - Статистика и события столов
- 📚 **Swagger документация** - Интерактивная API документация

## Требования

- Go 1.25+
- Docker и Docker Compose
- Make (опционально)
- Telegram Bot Token для авторизации

## Быстрый старт

### 1. Настройка переменных окружения

Скопируйте `.env` файл и укажите ваш Telegram Bot Token:

```env
BOT_TOKEN=your_telegram_bot_token_here
```

### 2. Запуск всех сервисов

```bash
# Запустить PostgreSQL, Kafka, Redis
make services-up
```

### 3. Запуск приложения

```bash
# В режиме разработки
make dev
```

Сервер запустится на порту 3000.

## 📚 API Документация

**Swagger UI доступен по адресу: http://localhost:3000/swagger/**

- Интерактивная документация всех endpoints
- Возможность тестирования API прямо в браузере
- Автоматическая авторизация через Telegram init_data
- Подробные схемы данных и примеры ответов

## Архитектура

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Telegram      │    │   Poker API     │    │   PostgreSQL    │
│   Web App       │◄──►│   (Go/Fiber)    │◄──►│   Database      │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                              │                         
                              ▼                         
                    ┌─────────────────┐    ┌─────────────────┐
                    │     Kafka       │    │     Redis       │
                    │   (Events)      │    │   (Cache)       │
                    └─────────────────┘    └─────────────────┘
```

## API Endpoints

### Публичные маршруты (без авторизации)

```
GET /api/v1/public/tables?category={ALL|LOW|MID|VIP}  - Получить столы
GET /api/v1/public/tables/:id                        - Получить стол
GET /api/v1/public/tables/:id/players                - Игроки за столом
```

### Защищенные маршруты (требуют авторизации)

#### Пользователи
```
GET /api/v1/profile                    - Профиль пользователя
PUT /api/v1/profile                    - Обновить профиль
GET /api/v1/my-tables                  - Мои столы
```

#### Столы
```
POST /api/v1/tables/:id/join           - Присоединиться к конкретному столу
POST /api/v1/tables/:id/leave          - Покинуть стол
POST /api/v1/join-available-table      - Присоединиться к доступному столу
GET /api/v1/available-tables           - Получить доступные столы
GET /api/v1/table-statistics           - Статистика столов
POST /api/v1/cleanup-empty-tables      - Очистить пустые столы
```

#### Игра
```
POST /api/v1/tables/:id/start-game     - Начать игру за столом
GET /api/v1/games/:gameId              - Состояние игры
POST /api/v1/games/:gameId/action      - Сделать ход
GET /api/v1/games/:gameId/history      - История игры
GET /api/v1/my-games                   - Мои активные игры
```

## Автоматическое управление столами

### Умное присоединение к столам
```bash
# Автоматически найти и присоединиться к лучшему доступному столу
curl -X POST -H "x-init-data: YOUR_INIT_DATA" \
  -H "Content-Type: application/json" \
  -d '{"category": "LOW"}' \
  http://localhost:3000/api/v1/join-available-table
```

### Автоматическое создание столов
- **При присоединении**: Создается новый стол, когда за текущим уже 2+ игрока
- **Менеджер столов**: Каждые 30 секунд проверяет и создает недостающие столы
- **Балансировка**: Игроки направляются в наиболее заполненные столы

### Статистика в реальном времени
```bash
curl -H "x-init-data: YOUR_INIT_DATA" \
  http://localhost:3000/api/v1/table-statistics
```

## Игровой процесс

### Быстрое присоединение к игре
```bash
# Автоматически присоединиться к лучшему доступному столу
curl -X POST -H "x-init-data: YOUR_INIT_DATA" \
  -H "Content-Type: application/json" \
  -d '{"category": "LOW"}' \
  http://localhost:3000/api/v1/join-available-table
```

### Ручное присоединение к столу
```bash
# Присоединиться к конкретному столу
curl -X POST -H "x-init-data: YOUR_INIT_DATA" \
  http://localhost:3000/api/v1/tables/1/join
```

### Начало игры
```bash
curl -X POST -H "x-init-data: YOUR_INIT_DATA" \
  http://localhost:3000/api/v1/tables/1/start-game
```

### Игровые действия
```bash
# Уравнять ставку
curl -X POST -H "x-init-data: YOUR_INIT_DATA" \
  -H "Content-Type: application/json" \
  -d '{"action": "call"}' \
  http://localhost:3000/api/v1/games/GAME_ID/action

# Повысить ставку
curl -X POST -H "x-init-data: YOUR_INIT_DATA" \
  -H "Content-Type: application/json" \
  -d '{"action": "raise", "amount": 100}' \
  http://localhost:3000/api/v1/games/GAME_ID/action

# Сброс карт
curl -X POST -H "x-init-data: YOUR_INIT_DATA" \
  -H "Content-Type: application/json" \
  -d '{"action": "fold"}' \
  http://localhost:3000/api/v1/games/GAME_ID/action
```

## Состояния игры

1. **waiting** - Ожидание начала
2. **preflop** - Префлоп (карты розданы)
3. **flop** - Флоп (3 общие карты)
4. **turn** - Терн (4-я карта)
5. **river** - Ривер (5-я карта)
6. **showdown** - Вскрытие карт
7. **finished** - Игра завершена

## События в реальном времени

### Kafka топики
- `poker-game-events` - События игры
- `poker-table-events` - События столов

### Типы событий
- `game_started` - Игра началась
- `player_action` - Действие игрока
- `game_state_changed` - Изменение состояния игры

## Категории столов

- **LOW** - Малые ставки (blinds: 1/2, buy-in: 50)
- **MID** - Средние ставки (blinds: 5/10, buy-in: 200)  
- **VIP** - Высокие ставки (blinds: 25/50, buy-in: 1000)

## Конфигурация

Настройки в файле `.env`:

```env
# База данных
DB_HOST=localhost
DB_PORT=5432
DB_USER=poker_user
DB_PASSWORD=poker_password
DB_NAME=poker_db

# Telegram
BOT_TOKEN=your_telegram_bot_token_here

# Kafka
KAFKA_BROKERS=localhost:9092

# Redis
REDIS_ADDR=localhost:6379
```

## Команды Make

```bash
make help           # Показать справку
make services-up    # Запустить все сервисы
make services-down  # Остановить все сервисы
make dev           # Запустить в режиме разработки
make build         # Собрать приложение
make test          # Запустить тесты
make clean         # Очистить сборку
```

## Структура проекта

```
poker/
├── cmd/main.go              # Точка входа
├── handlers/                # HTTP обработчики
│   ├── tables.go           # Столы
│   ├── users.go            # Пользователи
│   └── game.go             # Игра
├── middleware/auth.go       # Авторизация
├── models/models.go         # Модели данных
├── services/               # Сервисы
│   ├── kafka.go           # Kafka
│   └── redis.go           # Redis
├── game/poker.go           # Игровая логика
├── database/connection.go   # База данных
├── docker-compose.yml      # Docker сервисы
└── init.sql               # Инициализация БД
```

## Особенности реализации

### Игровая логика
- Полная реализация Texas Hold'em
- Определение всех покерных комбинаций
- Автоматическое определение победителя
- Поддержка all-in и side pots

### Безопасность
- Валидация Telegram init_data
- Проверка подписи HMAC-SHA256
- Контроль доступа к игровым действиям
- Транзакционная безопасность

### Производительность
- Кэширование состояний игры в Redis
- Асинхронные события через Kafka
- Оптимизированные запросы к БД
- Индексы для быстрого поиска

## Документация

- 📚 **[Swagger UI](http://localhost:3000/swagger/)** - Интерактивная API документация
- `swagger-docs.md` - Руководство по использованию Swagger
- `auth-examples.md` - Примеры авторизации
- `game-api-docs.md` - Подробная документация игрового API
- `table-management-docs.md` - Управление столами и автоматизация
- `test_api.sh` - Скрипт для тестирования API
- `test_game.sh` - Тестирование игрового процесса
- `test_table_management.sh` - Тестирование управления столами

## Тестирование

```bash
# Запустить все сервисы
make services-up

# Запустить приложение
make dev

# Тестировать API
./test_api.sh
```

Подробные примеры использования см. в `game-api-docs.md`.